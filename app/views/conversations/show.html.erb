<h1><%= @conversation.title %></h1>
<br>
<h3> Messages </h3>
<ul>
    <% @conversation.messages.each do |message| %>
        <li><%= link_to message.text, message_path(message) %></li>
    <% end %>
</ul>

<%= button_to "All Conversations", conversations_path, method: :get %>